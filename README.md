xai
=====

## Examples

Kling image and video generation:

```bash
go run ./examples/kling          # models, text2image, image2image, text2video, img2video, keyframe, multi_ref
```

Set `QINIU_API_KEY` for real API calls; omit for mock mode. See [examples/README.md](examples/README.md).
